# Sanitized fixtures only

Fixtures in this repository must be synthetic or manually redacted. Allowed
fields are status, non-sensitive headers, declared charset, response category,
and short synthetic bodies that exercise a parser contract.

Never add real Cookie/Set-Cookie values, account identifiers, passwords,
captcha answers, private messages, post bodies, AI keys, signing files, or
tokens. Live probe output is not written here automatically.

